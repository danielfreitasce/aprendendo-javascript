var n1 = Number(window.prompt('Digite um número:'))
var n2 = Number(window.prompt('Digite outro número:'))
var soma = n1 + n2
window.alert(`A soma dos números é ${soma}`)

/** A função window.prompt mostra uma caixinha onde você pode inserir dados. 
 * Porém caso estiver querendo receber um número terá que converter em número,
 * pois essa função somente recebe strings. 
 * 
 * Para converter em número, basta envolver a função dentro da função Number. 
 * 
 * Eu falo tanto de função, mas o que é função? 
 * Função são linhas de código prontas que executam uma determinada tarefa. 
 * Existem centenas ou até milhares de funções prontas dentro de uma linguagem de programação. 
 * Caso este seja seu primeiro contato com programção, 
 * sugiro assistir às vídeo aulas de JavaScript do canal Curso em Vídeo com o Gustavo Guanabara. 
 */